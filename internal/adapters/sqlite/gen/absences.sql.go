// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: absences.sql

package db

import (
	"context"
	"database/sql"
)

const createAbsence = `-- name: CreateAbsence :one
INSERT INTO absences (date, type, fraction, note) VALUES (?, ?, ?, ?) RETURNING id
`

type CreateAbsenceParams struct {
	Date     string
	Type     string
	Fraction float64
	Note     sql.NullString
}

func (q *Queries) CreateAbsence(ctx context.Context, arg CreateAbsenceParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, createAbsence,
		arg.Date,
		arg.Type,
		arg.Fraction,
		arg.Note,
	)
	var id int64
	err := row.Scan(&id)
	return id, err
}

const deleteAbsence = `-- name: DeleteAbsence :exec
DELETE FROM absences WHERE id = ?
`

func (q *Queries) DeleteAbsence(ctx context.Context, id int64) error {
	_, err := q.db.ExecContext(ctx, deleteAbsence, id)
	return err
}

const listAbsencesBetween = `-- name: ListAbsencesBetween :many
SELECT id, date, type, fraction, note FROM absences WHERE date BETWEEN ?1 AND ?2 ORDER BY date
`

type ListAbsencesBetweenParams struct {
	FromDate string
	ToDate   string
}

func (q *Queries) ListAbsencesBetween(ctx context.Context, arg ListAbsencesBetweenParams) ([]Absence, error) {
	rows, err := q.db.QueryContext(ctx, listAbsencesBetween, arg.FromDate, arg.ToDate)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Absence
	for rows.Next() {
		var i Absence
		if err := rows.Scan(
			&i.ID,
			&i.Date,
			&i.Type,
			&i.Fraction,
			&i.Note,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
